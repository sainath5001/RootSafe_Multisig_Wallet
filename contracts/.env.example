# Rootstock Testnet RPC URL
ROOTSTOCK_TESTNET_RPC=https://public-node.testnet.rsk.co

# Deployer private key
# IMPORTANT: Never commit your actual private key to version control.
# Pass this to Foundry via --private-key (see contracts/README.md).
PRIVATE_KEY=0xYOUR_PRIVATE_KEY_HERE

# Optional: Custom owners (comma-separated addresses)
# If not set, the deployment script can fall back to default test addresses.
OWNERS=0xYourOwner1Address,0xYourOwner2Address,0xYourOwner3Address

# Optional: Required confirmations (default: 2)
# Must be between 1 and the number of owners.
REQUIRED_CONFIRMATIONS=2
